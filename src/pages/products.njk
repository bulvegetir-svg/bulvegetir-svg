---
title: "Ürünler"
pageClass: "products-page"
layout: layout                 # Eleventy, src/_includes/layout.njk dosyasını kullanır
permalink: "/products/"        # Çıktı yolu: dist/products/index.html
---
{% block content %}
  <section class="products-section">
    <h1>Ürün Kataloğu</h1>

    {# Eleventy koleksiyonundan “products” adıyla gelen verileri döngüyle gösteriyoruz #}
    <ul class="product-list">
      {% for product in collections.products.products %}   {# collections.products.products  → products.json dosyasındaki dizi #}
        <li class="product-item">
          <h2>{{ product.name }}</h2>
          <p>Fiyat: <strong>{{ product.price | currency }}</strong> TL</p>
        </li>
      {% else %}
        <li>Şu anda listelenecek ürün yok.</li>
      {% endfor %}
    </ul>
  </section>
{% endblock %}
