---
layout: layout                     # src/_includes/layout.njk kullanır
pagination:
  data: "collections.products"      # koleksiyon artık doğrudan dizi
  size: 1                           # bir öğe başına bir sayfa
  alias: product                    # şablonda bu obje `product` adıyla erişilir
permalink: "/product/{{ product.slug }}/"   # /product/<slug>/ yoluna çıktı
eleventyComputed:
  title: "{{ product.name }}"       # sayfa başlığı dinamik olarak ürün adı
---
{% block content %}
  <section class="product-detail">
    <h1>{{ product.name }}</h1>

    <p>Fiyat: <strong>{{ product.price | currency }}</strong> TL</p>

    <a href="/products/" class="button secondary">Tüm Ürünler</a>
  </section>
{% endblock %}
