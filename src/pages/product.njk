---
layout: layout                     # src/_includes/layout.njk kullanılacak
pagination:
  data: "collections.products.products"   # Eleventy koleksiyonundan diziyi al
  size: 1                         # Tek bir öğe başına bir sayfa
  alias: product                   # Şablonda bu obje `product` adıyla erişilir
permalink: "/product/{{ product.slug }}/"   # Çıktı yolu: /product/<slug>/
eleventyComputed:
  title: "{{ product.name }}"      # Sayfa başlığı dinamik olarak ürün adı
---
{% block content %}
  <section class="product-detail">
    <h1>{{ product.name }}</h1>

    <p>Fiyat: <strong>{{ product.price | currency }}</strong> TL</p>

    <a href="/products/" class="button secondary">Tüm Ürünler</a>
  </section>
{% endblock %}
