name: Deploy to Vercel

on:
  push:
    branches: [ main ]   # yalnızca main branch’inde çalışır

jobs:
  vercel-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # (Opsiyonel) Node.js kurulumu – projenizde npm paketleri varsa
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'   # tercihe göre versiyon

      - name: Install dependencies
        run: npm ci   # paketleri kurar

      - name: Build project
        run: npm run build   # Eleventy → dist/ klasörünü üretir

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}   # ← secret
          vercel-args: '--prod --prebuilt'           # prod ortamı + prebuilt flag
          framework: false                           # Vercel framework algılamasını devre dışı bırak
          # vercel-org-id ve vercel-project-id isteğe bağlıdır; doğru değillerse yorum satırı bırakılabilir
          # vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          # vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
